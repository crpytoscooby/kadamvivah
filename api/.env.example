# ==============================================================================
# KadamVivah - API Environment Configuration
# Copy this file to `.env` in the `api/` or project root directory
# ==============================================================================

# Application Environment (development | production)
APP_ENV=production
APP_DEBUG=false

# Database Configuration (Hostinger MySQL / MariaDB)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=kadamvivah
DB_USER=root
DB_PASS=

# Allowed CORS Origin (Frontend URL)
CORS_ORIGIN=http://localhost:5173
